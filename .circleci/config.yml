version: 2
jobs:
  build:
    machine:
      - image: circleci/php:5.4.15
    steps:
     - checkout
     #- run: echo "A first hello"
  #deploy:
  #  machine:
  #    enabled: true
  #  steps:
  #    - run:
  #        name: Deploy Over SSH
  #        command: |
  #          ssh $SSH_USER@$SSH_HOST "<remote deploy command>"

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
          filters:
            branches:
              only: /.*/
      #- deploy:
      #    requires:
      #      - build
      #    filters:
      #      branches:
      #        only: master
